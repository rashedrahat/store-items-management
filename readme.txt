This is a not a GUI application. To run this app open via browser > inspect > console > run.
Or, this app can be run onto any online platform where JavaScript execution supports.
Last but not the least, this app will be run on any device if the device has a JavaScript code execution engine like Google Chrome V8, etc.
-------------------------------------------------------------------------------------------------------------------------------------------
Following codes have to use to see the outputs of this app:
#1. myStore.name;
#2. myStore.isItemAvailable("shirt");
Or, myStore.isItemAvailable("underware");
#3. myStore.getPrice("shirt");
Or, myStore.getPrice("underware");
#4. myStore.getTotalSale();
#5. myStore.sellItem("shirt", 10);
#6. myStore.addItem("shoes", "500", "100");